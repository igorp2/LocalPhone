@page
@model LocalPhoneAdmin.Areas.City.DetailsModel

@using Microsoft.AspNetCore.Identity
@using LocalPhoneDomain.Areas.Identity.Data

@inject SignInManager<UserModel> SignInManager
@inject UserManager<UserModel> UserManager

@if (SignInManager.IsSignedIn(User))
{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";

    <h1>Details</h1>

    <div>
        <h4>CityModel</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.CityModel.Phonecode)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.CityModel.Phonecode)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.CityModel.Description)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.CityModel.Description)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.CityModel.State)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.CityModel.State.Name)
            </dd>
        </dl>
    </div>
    <div>
        <a asp-page="./Edit" asp-route-id="@Model.CityModel.Id">Edit</a> |
        <a asp-page="./Index">Back to List</a>
    </div>
}