@page
@model LocalPhoneAdmin.Areas.AvailableNumber.Pages.DeleteModel

@using Microsoft.AspNetCore.Identity
@using LocalPhoneDomain.Areas.Identity.Data

@inject SignInManager<UserModel> SignInManager
@inject UserManager<UserModel> UserManager

@if (SignInManager.IsSignedIn(User))
{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";

    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>AvailableNumberModel</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.AvailableNumberModel.idCountry)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.AvailableNumberModel.idCountry)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.AvailableNumberModel.idCity)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.AvailableNumberModel.idCity)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.AvailableNumberModel.phoneNumber)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.AvailableNumberModel.phoneNumber)
            </dd>
            @*<dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.GenderModel.IdCountry)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.GenderModel.Country.Nicename)
            </dd>*@
        </dl>
        <form method="post">
            <input type="hidden" asp-for="AvailableNumberModel.Id" />
            <input asp-for="AvailableNumberModel.Status" class="form-control" type="hidden" />
            <input asp-for="AvailableNumberModel.CreationDate" class="form-control" type="hidden" />
            <input asp-for="AvailableNumberModel.CreatorUser" class="form-control" type="hidden" />
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            <a asp-page="./Index">Back to List</a>
        </form>
    </div>
}